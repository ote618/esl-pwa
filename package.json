{
  "name": "esl-pwa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:data && vite",
    "build": "npm run build:data && vite build",
    "preview": "vite preview",
    "build:data": "node scripts/build-registry.mjs",
    "build:data:fixtures": "node scripts/build-registry.mjs --fixtures",
    "build:data:strict": "node scripts/build-registry.mjs --fixtures --require-manifest",
    "test:data": "node scripts/test-validators.mjs",
    "build:docs": "node scripts/build-docs.mjs",
    "build:board": "node scripts/build-tracker.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "vite-plugin-pwa": "^0.21.1"
  }
}
